---
enable_worker_cronjobs: &enable_cron false
enable_purge_cronjobs: &enable_purge false
disable_worker_email: &disable_email true

dlrn::web::web_domain: 'trunk.rdoproject.org'
dlrn::web::enable_api: true
dlrn::web::api_workers: ['centos-master-uc', 'centos-rocky', 'centos-queens', 'centos-pike', 'centos-ocata', 'centos-newton']
dlrn::web::cert_mail: undef

dlrn::workers:
  centos-master:
    distro: 'centos7'
    target: 'centos'
    distgit_branch: 'rpm-master-head'
    distro_branch: 'master'
    disable_email: *disable_email
    enable_cron: *enable_cron
    cron_env: '--head-only'
    cron_hour: '*/4'
    cron_minute: '5'
    enable_purge: *enable_purge
    purge_hour: '1'
    purge_minute: '7'
    symlinks: ['/var/www/html/centos7-master-head']
    release: 'stein'
    baseurl: 'https://trunk.rdoproject.org/centos7-master-head'
    pkginfo_driver: 'dlrn.drivers.rdoinfo.RdoInfoDriver'
    build_driver: 'dlrn.drivers.mockdriver.MockBuildDriver'
    db_connection: 'sqlite:///commits.sqlite'
    mock_tmpfs_enable: true
    fallback_to_master: true
    enable_deps_sync: false
    enable_brs_sync: false
#   gerrit_user: 'rdo-trunk'
#   gerrit_email: 'rdo-trunk@rdoproject.org'
#   gerrit_topic: 'rdo-FTBFS-master-head'
#   rsyncdest: 'centos-master@backupserver.example.com:/home/centos-master/data/repos'
#   rsyncport: '3000'
  centos-master-uc:
    distro: 'centos7'
    target: 'centos-master-uc'
    distgit_branch: 'rpm-master'
    distro_branch: 'master'
    disable_email: *disable_email
    enable_cron: *enable_cron
    enable_purge: *enable_purge
    purge_hour: '2'
    purge_minute: '7'
    symlinks: ['/var/www/html/centos7', '/var/www/html/centos70', '/var/www/html/centos7-master' ]
    release: 'stein-uc'
    baseurl: 'https://trunk.rdoproject.org/centos7'
    pkginfo_driver: 'dlrn.drivers.rdoinfo.RdoInfoDriver'
    build_driver: 'dlrn.drivers.mockdriver.MockBuildDriver'
    db_connection: 'sqlite:///commits.sqlite'
    mock_tmpfs_enable: true
    fallback_to_master: true
    enable_deps_sync: true
    enable_brs_sync: true
#   gerrit_user: 'rdo-trunk'
#   gerrit_email: 'rdo-trunk@rdoproject.org'
#   gerrit_topic: 'rdo-FTBFS'
  centos-rocky:
    distro: 'centos7'
    target: 'centos-rocky'
    distgit_branch: 'rocky-rdo'
    distro_branch: 'stable/rocky'
    disable_email: *disable_email
    enable_cron: *enable_cron
    enable_purge: *enable_purge
    purge_hour: '4'
    purge_minute: '7'
    symlinks: ['/var/www/html/centos7-rocky', '/var/www/html/rocky/centos7']
# NOTE(jpena): this will change to rocky once the stein-uc and stein tags are in rdoinfo
    release: 'rocky'
# TODO(jpena): remove bootstrap suffix once the initial bootstrap is completed
    baseurl: 'https://trunk.rdoproject.org/centos7-rocky-bootstrap'
    pkginfo_driver: 'dlrn.drivers.rdoinfo.RdoInfoDriver'
    build_driver: 'dlrn.drivers.mockdriver.MockBuildDriver'
    db_connection: 'sqlite:///commits.sqlite'
    mock_tmpfs_enable: true
# TODO(jpena): set to false once all queens-rdo branches have been created, then cleanup rdoinfo
# to ensure all packages are built
    fallback_to_master: true
# TODO(jpena): we should enable this once master has switched to Stein
    enable_deps_sync: false
    enable_brs_sync: false
#   gerrit_user: 'rdo-trunk'
#   gerrit_email: 'rdo-trunk@rdoproject.org'
#   gerrit_topic: 'rdo-FTBFS-rocky'
  centos-queens:
    distro: 'centos7'
    target: 'centos-queens'
    distgit_branch: 'queens-rdo'
    distro_branch: 'stable/queens'
    disable_email: *disable_email
    enable_cron: *enable_cron
    enable_purge: *enable_purge
    purge_hour: '1'
    purge_minute: '7'
    symlinks: ['/var/www/html/centos7-queens', '/var/www/html/queens/centos7']
    release: 'queens'
    baseurl: 'https://trunk.rdoproject.org/centos7-queens'
    pkginfo_driver: 'dlrn.drivers.rdoinfo.RdoInfoDriver'
    build_driver: 'dlrn.drivers.mockdriver.MockBuildDriver'
    db_connection: 'sqlite:///commits.sqlite'
    mock_tmpfs_enable: true
    fallback_to_master: false
    enable_deps_sync: true
    enable_brs_sync: false
#   gerrit_user: 'rdo-trunk'
#   gerrit_email: 'rdo-trunk@rdoproject.org'
#   gerrit_topic: 'rdo-FTBFS-queens'
  centos-pike:
    distro: 'centos7'
    target: 'centos-pike'
    distgit_branch: 'pike-rdo'
    distro_branch: 'stable/pike'
    disable_email: *disable_email
    enable_cron: *enable_cron
    enable_purge: *enable_purge
    purge_hour: '0'
    purge_minute: '7'
    symlinks: ['/var/www/html/centos7-pike', '/var/www/html/pike/centos7']
    release: 'pike'
    baseurl: 'https://trunk.rdoproject.org/centos7-pike'
    pkginfo_driver: 'dlrn.drivers.rdoinfo.RdoInfoDriver'
    build_driver: 'dlrn.drivers.mockdriver.MockBuildDriver'
    db_connection: 'sqlite:///commits.sqlite'
    mock_tmpfs_enable: true
    fallback_to_master: false
    enable_deps_sync: false
    enable_brs_sync: false
#   gerrit_user: 'rdo-trunk'
#   gerrit_email: 'rdo-trunk@rdoproject.org'
#   gerrit_topic: 'rdo-FTBFS-pike'
  centos-ocata:
    distro: 'centos7'
    target: 'centos-ocata'
    distgit_branch: 'ocata-rdo'
    distro_branch: 'stable/ocata'
    disable_email: *disable_email
    enable_cron: *enable_cron
    enable_purge: *enable_purge
    purge_hour: '2'
    purge_minute: '37'
    symlinks: ['/var/www/html/centos7-ocata', '/var/www/html/ocata/centos7']
    release: 'ocata'
    baseurl: 'https://trunk.rdoproject.org/centos7-ocata'
    pkginfo_driver: 'dlrn.drivers.rdoinfo.RdoInfoDriver'
    build_driver: 'dlrn.drivers.mockdriver.MockBuildDriver'
    db_connection: 'sqlite:///commits.sqlite'
    mock_tmpfs_enable: false
    fallback_to_master: false
    enable_deps_sync: false
    enable_brs_sync: false
#   gerrit_user: 'rdo-trunk'
#   gerrit_email: 'rdo-trunk@rdoproject.org'
#   gerrit_topic: 'rdo-FTBFS-ocata'
  centos-newton:
    distro: 'centos7'
    target: 'centos-newton'
    distgit_branch: 'newton-rdo'
    distro_branch: 'stable/newton'
    disable_email: *disable_email
    enable_cron: *enable_cron
    cron_hour: '*/2'
    cron_minute: '17'
    enable_purge: *enable_purge
    purge_hour: '3'
    purge_minute: '7'
    symlinks: ['/var/www/html/centos7-newton', '/var/www/html/newton/centos7']
    release: 'newton'
    baseurl: 'https://trunk.rdoproject.org/centos7-newton'
    pkginfo_driver: 'dlrn.drivers.rdoinfo.RdoInfoDriver'
    build_driver: 'dlrn.drivers.mockdriver.MockBuildDriver'
    db_connection: 'sqlite:///commits.sqlite'
    mock_tmpfs_enable: false
    fallback_to_master: false
    enable_deps_sync: false
    enable_brs_sync: false
  fedora-master:
    distro: 'f28'
    target: 'fedora'
    distgit_branch: 'rpm-master'
    distro_branch: 'master'
    disable_email: *disable_email
    enable_cron: *enable_cron
    enable_purge: *enable_purge
    purge_hour: '6'
    purge_minute: '7'
    symlinks: ['/var/www/html/f28', '/var/www/html/fedora28', '/var/www/html/fedora']
    release: 'stein-py3-uc'
    baseurl: 'https://trunk.rdoproject.org/fedora'
    pkginfo_driver: 'dlrn.drivers.rdoinfo.RdoInfoDriver'
    build_driver: 'dlrn.drivers.mockdriver.MockBuildDriver'
    db_connection: 'sqlite:///commits.sqlite'
    mock_tmpfs_enable: false
    fallback_to_master: true
    enable_deps_sync: false
    enable_brs_sync: false
