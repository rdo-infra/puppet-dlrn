diff -Naur a/sh.py b/sh.py
--- a/sh.py	2015-11-20 17:11:01.220066511 +0100
+++ b/sh.py	2015-11-20 17:11:50.163454860 +0100
@@ -1531,7 +1531,9 @@
     def signal(self, sig):
         self.log.debug("sending signal %d", sig)
         try:
-            os.kill(self.pid, sig)
+            # Kill the entire process group as the command may have
+            # spawned subprocesses we don't want left around
+            os.killpg(os.getpgid(self.pid), sig)
         except OSError:
             pass
 
